class NotificationService:
    
    def send_notification(self, message):
        # You can add logic for sending notifications, e.g., email, SMS, etc.
        print(f"Sending notification: {message}")
